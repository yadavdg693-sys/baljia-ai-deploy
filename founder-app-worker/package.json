{
  "name": "baljia-founder-apps",
  "version": "0.1.0",
  "description": "Wildcard Cloudflare Worker that serves founder landing pages at *.baljia.app by reading R2 by Host-derived subdomain (ADR-002 Shape 1).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:remote": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250301.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
